--- # This is Variable Pratice test
  - hosts: lab
    user:
    become: yes
    connection: ssh
    vars:
      pkgname: httpd
    tasks:
      - name: httpd installation on All Slave nodes
        action: yum name='{{pkgname}}' state=present
